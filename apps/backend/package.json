{
  "name": "backend",
  "version": "1.0.0",
  "private": true,
  "main": "lib/index.js",
  "scripts": {
    "dev": "tsc --watch",
    "start": "node lib/infrastructure/entrypoints/api/server.js",
    "build": "npm run clean && tsc -b",
    "clean": "rm -rf lib coverage",
    "compile": "tsc --noEmit",
    "test": "jest --verbose"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/supertest": "^7.2.0",
    "supertest": "^7.2.2"
  }
}
